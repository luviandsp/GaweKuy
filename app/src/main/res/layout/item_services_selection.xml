<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/iv_delete_service"
        android:layout_width="28dp"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:src="@drawable/cross_circle_delete"/>

    <EditText
        android:id="@+id/et_service_name"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        app:fontFamily="@font/urbanist_semibold"
        android:textSize="16sp"
        android:hint="@string/service_name_selection"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="Rp."/>

    <EditText
        android:id="@+id/et_service_price"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:hint="@string/price"
        app:fontFamily="@font/urbanist_semibold"
        android:textSize="16sp"
        android:inputType="number"/>
</LinearLayout>
