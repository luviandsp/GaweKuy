<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GaweKuy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="fontFamily">@font/urbanist</item>
        <item name="android:windowBackground">@color/dark_blue</item>
        <item name="android:statusBarColor">@color/dark_grey</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/dark_grey</item>
        <item name="android:colorControlActivated">@color/blue</item>
        <item name="android:textColorHighlight">@color/light_grey</item>
    </style>
</resources>